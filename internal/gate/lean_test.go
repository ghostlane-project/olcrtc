//go:build olcrtc_lean

package gate

// ai-generated: whole file, the lean half of the build switch the flavour
// tests read (the pattern internal/e2e uses).

// leanBuild says this test binary was built with olcrtc_lean, the phones'
// build, which is the only one that carries the mobile flavour.
const leanBuild = true
